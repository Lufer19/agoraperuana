import React, { useState } from 'react';
import { Menu, X, Smartphone, Tablet, Monitor, Check } from 'lucide-react';

const DisenoResponsive = () => {
  const [menuAbierto, setMenuAbierto] = useState(false);
  const [vistaDemo, setVistaDemo] = useState('desktop');

  const caracteristicas = [
    {
      icono: '📱',
      titulo: 'Mobile First',
      descripcion: 'Diseñado primero para móviles, luego adaptado a pantallas más grandes'
    },
    {
      icono: '🎨',
      titulo: 'Diseño Fluido',
      descripcion: 'Los elementos se adaptan proporcionalmente al tamaño de pantalla'
    },
    {
      icono: '⚡',
      titulo: 'Carga Rápida',
      descripcion: 'Optimizado para dispositivos móviles con conexiones lentas'
    },
    {
      icono: '👆',
      titulo: 'Touch Friendly',
      descripcion: 'Botones y enlaces con tamaño mínimo de 44x44px para táctil'
    },
    {
      icono: '🔄',
      titulo: 'Menú Adaptativo',
      descripcion: 'Hamburger menu en móvil, navegación completa en desktop'
    },
    {
      icono: '📐',
      titulo: 'Grid System',
      descripcion: 'Sistema de rejilla que se reorganiza según el dispositivo'
    }
  ];

  const breakpoints = [
    { nombre: 'Mobile', rango: '0 - 640px', descripcion: '1 columna, menú hamburguesa' },
    { nombre: 'Tablet', rango: '641px - 1024px', descripcion: '2 columnas, navegación compacta' },
    { nombre: 'Desktop', rango: '1025px+', descripcion: '3-4 columnas, navegación completa' }
  ];

  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      {/* Header Responsive */}
      <header className="bg-white shadow-md sticky top-0 z-50">
        <div className="container mx-auto px-4">
          <div className="flex items-center justify-between h-16">
            {/* Logo */}
            <div className="flex items-center">
              <div className="w-10 h-10 bg-gradient-to-r from-indigo-600 to-purple-600 rounded-lg flex items-center justify-center text-white font-bold">
                F
              </div>
              <span className="ml-3 text-xl font-bold text-gray-800 hidden sm:block">
                Filosofía Total
              </span>
            </div>

            {/* Navegación Desktop */}
            <nav className="hidden md:flex space-x-6">
              {['Inicio', 'Períodos', 'Glosario', 'Tests', 'Recursos'].map(item => (
                <a
                  key={item}
                  href="#"
                  className="text-gray-700 hover:text-indigo-600 font-medium transition"
                >
                  {item}
                </a>
              ))}
            </nav>

            {/* Botón Desktop */}
            <button className="hidden md:block bg-indigo-600 text-white px-6 py-2 rounded-lg hover:bg-indigo-700 transition">
              Acceder
            </button>

            {/* Menú Hamburguesa Mobile */}
            <button
              className="md:hidden p-2 rounded-lg hover:bg-gray-100 transition"
              onClick={() => setMenuAbierto(!menuAbierto)}
            >
              {menuAbierto ? (
                <X className="w-6 h-6 text-gray-700" />
              ) : (
                <Menu className="w-6 h-6 text-gray-700" />
              )}
            </button>
          </div>

          {/* Menú Mobile Expandible */}
          {menuAbierto && (
            <nav className="md:hidden py-4 border-t animate-slideDown">
              {['Inicio', 'Períodos', 'Glosario', 'Tests', 'Recursos'].map(item => (
                <a
                  key={item}
                  href="#"
                  className="block py-3 px-4 text-gray-700 hover:bg-indigo-50 hover:text-indigo-600 transition rounded-lg"
                >
                  {item}
                </a>
              ))}
              <button className="w-full mt-3 bg-indigo-600 text-white py-3 rounded-lg hover:bg-indigo-700 transition">
                Acceder
              </button>
            </nav>
          )}
        </div>
      </header>

      <div className="container mx-auto px-4 py-8">
        {/* Hero Section Responsive */}
        <section className="bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-2xl p-6 md:p-12 mb-8 shadow-xl">
          <div className="max-w-3xl">
            <h1 className="text-3xl md:text-5xl lg:text-6xl font-bold mb-4">
              Diseño Responsive
            </h1>
            <p className="text-lg md:text-xl opacity-90 mb-6">
              Ejemplo completo de cómo adaptar tu sitio web de filosofía a todos los dispositivos
            </p>
            <div className="flex flex-col sm:flex-row gap-3">
              <button className="bg-white text-indigo-600 px-6 py-3 rounded-lg font-bold hover:bg-gray-100 transition">
                Ver Demo
              </button>
              <button className="border-2 border-white text-white px-6 py-3 rounded-lg font-bold hover:bg-white/10 transition">
                Documentación
              </button>
            </div>
          </div>
        </section>

        {/* Selector de Vista Demo */}
        <div className="bg-white rounded-2xl shadow-lg p-6 mb-8">
          <h2 className="text-2xl font-bold mb-4 text-gray-800">
            Vista Previa por Dispositivo
          </h2>
          <div className="flex flex-wrap gap-3 mb-6">
            {[
              { id: 'mobile', icon: Smartphone, label: 'Móvil' },
              { id: 'tablet', icon: Tablet, label: 'Tablet' },
              { id: 'desktop', icon: Monitor, label: 'Desktop' }
            ].map(vista => {
              const Icon = vista.icon;
              return (
                <button
                  key={vista.id}
                  onClick={() => setVistaDemo(vista.id)}
                  className={`flex items-center px-4 py-3 rounded-lg font-medium transition ${
                    vistaDemo === vista.id
                      ? 'bg-indigo-600 text-white'
                      : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
                  }`}
                >
                  <Icon className="w-5 h-5 mr-2" />
                  {vista.label}
                </button>
              );
            })}
          </div>

          {/* Simulación de vistas */}
          <div className="bg-gray-100 rounded-xl p-8 flex justify-center">
            <div
              className={`bg-white rounded-lg shadow-2xl overflow-hidden transition-all duration-500 ${
                vistaDemo === 'mobile'
                  ? 'w-80 h-96'
                  : vistaDemo === 'tablet'
                  ? 'w-full max-w-2xl h-96'
                  : 'w-full h-96'
              }`}
            >
              <div className="h-8 bg-gradient-to-r from-indigo-600 to-purple-600 flex items-center px-4">
                <div className="flex space-x-2">
                  <div className="w-3 h-3 bg-red-400 rounded-full"></div>
                  <div className="w-3 h-3 bg-yellow-400 rounded-full"></div>
                  <div className="w-3 h-3 bg-green-400 rounded-full"></div>
                </div>
              </div>
              <div className="p-4 overflow-auto h-full">
                <div className={`grid gap-4 ${
                  vistaDemo === 'mobile' ? 'grid-cols-1' :
                  vistaDemo === 'tablet' ? 'grid-cols-2' :
                  'grid-cols-3'
                }`}>
                  {[1, 2, 3, 4, 5, 6].slice(0, vistaDemo === 'mobile' ? 3 : 6).map(i => (
                    <div key={i} className="bg-gray-50 p-4 rounded-lg">
                      <div className="w-full h-20 bg-gradient-to-r from-indigo-200 to-purple-200 rounded mb-2"></div>
                      <div className="h-3 bg-gray-200 rounded mb-2"></div>
                      <div className="h-3 bg-gray-200 rounded w-3/4"></div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* Características Responsive */}
        <section className="mb-8">
          <h2 className="text-3xl font-bold mb-6 text-gray-800 text-center">
            Características del Diseño Responsive
          </h2>
          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {caracteristicas.map((item, idx) => (
              <div
                key={idx}
                className="bg-white p-6 rounded-xl shadow-md hover:shadow-xl transition"
              >
                <div className="text-5xl mb-4">{item.icono}</div>
                <h3 className="text-xl font-bold mb-2 text-gray-800">
                  {item.titulo}
                </h3>
                <p className="text-gray-600">{item.descripcion}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Breakpoints */}
        <section className="bg-white rounded-2xl shadow-lg p-6 md:p-8 mb-8">
          <h2 className="text-3xl font-bold mb-6 text-gray-800">
            Breakpoints y Adaptaciones
          </h2>
          <div className="space-y-4">
            {breakpoints.map((bp, idx) => (
              <div
                key={idx}
                className="border-l-4 border-indigo-500 pl-6 py-4 bg-indigo-50 rounded-r-lg"
              >
                <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-2">
                  <h3 className="text-xl font-bold text-indigo-800">
                    {bp.nombre}
                  </h3>
                  <span className="text-sm font-mono bg-indigo-200 text-indigo-800 px-3 py-1 rounded-full">
                    {bp.rango}
                  </span>
                </div>
                <p className="text-gray-700">{bp.descripcion}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Código de Ejemplo */}
        <section className="bg-gray-900 text-green-400 rounded-2xl p-6 md:p-8 shadow-lg overflow-x-auto">
          <h3 className="text-2xl font-bold mb-4 text-white">
            💻 Código Tailwind CSS
          </h3>
          <pre className="text-sm font-mono">
            <code>{`<!-- Grid Responsive -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <div class="bg-white p-4 rounded-lg">Card 1</div>
  <div class="bg-white p-4 rounded-lg">Card 2</div>
  <div class="bg-white p-4 rounded-lg">Card 3</div>
</div>

<!-- Texto Responsive -->
<h1 class="text-2xl md:text-4xl lg:text-5xl font-bold">
  Título Adaptativo
</h1>

<!-- Padding Responsive -->
<div class="p-4 md:p-8 lg:p-12">
  Contenido con padding adaptativo
</div>

<!-- Ocultar/Mostrar según dispositivo -->
<div class="hidden md:block">Solo visible en desktop</div>
<div class="md:hidden">Solo visible en mobile</div>

<!-- Flexbox Responsive -->
<div class="flex flex-col md:flex-row gap-4">
  <div>Columna 1</div>
  <div>Columna 2</div>
</div>`}</code>
          </pre>
        </section>

        {/* Checklist */}
        <section className="mt-8 bg-gradient-to-r from-green-500 to-emerald-600 text-white rounded-2xl p-6 md:p-8 shadow-lg">
          <h2 className="text-3xl font-bold mb-6">
            ✅ Checklist de Diseño Responsive
          </h2>
          <div className="grid md:grid-cols-2 gap-4">
            {[
              'Menú hamburguesa en móvil',
              'Imágenes con max-width: 100%',
              'Tipografía escalable (rem/em)',
              'Touch targets mínimo 44x44px',
              'Contenido legible sin zoom',
              'Formularios adaptados a móvil',
              'Tablas con scroll horizontal',
              'Meta viewport configurado',
              'Carga rápida en 3G',
              'Testeo en dispositivos reales'
            ].map((item, idx) => (
              <div key={idx} className="flex items-start">
                <Check className="w-6 h-6 mr-3 flex-shrink-0 mt-0.5" />
                <span className="text-lg">{item}</span>
              </div>
            ))}
          </div>
        </section>
      </div>

      <style jsx>{`
        @keyframes slideDown {
          from {
            opacity: 0;
            transform: translateY(-10px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
        .animate-slideDown {
          animation: slideDown 0.3s ease-out;
        }
      `}</style>
    </div>
  );
};

export default DisenoResponsive;
